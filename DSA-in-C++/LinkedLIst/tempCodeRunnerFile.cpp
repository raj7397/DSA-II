
     LinkedList * ans = new LinkedList();
    
    ans = addTwoList(list->head,list1->head)
    cout<<"Ans display"<<endl;
    ans.display(ans->head);
   // list->head = list->addBegin(list->head, 9);
    //list->head = list->addBegin(list->head, 9);
}